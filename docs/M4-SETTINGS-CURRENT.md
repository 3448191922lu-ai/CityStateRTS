# M4-02 音量与显示设置候选

## 当前状态

M4-02 的代码、配置、专项测试、Windows 打包、启动烟雾检查和人工验证已经完成，当前状态为“完成”。候选包：

`D:\ue project\RTS\Builds\Windows_M4_Settings\RTS.exe`

暂停菜单关闭后的鼠标捕获已修正：恢复战局时使用适合可见光标 RTS 的 `GameAndUI` 输入模式，并主动释放菜单按钮的指针捕获。用户已确认点击“继续游戏”后左键选择、右键移动恢复正常。

## 已实现设置

- `Esc` 打开暂停页并暂停世界，继续、设置和退出按钮由原生 UMG 菜单提供。
- 总音量按 5% 步长调整并持久化。
- 窗口、无边框全屏和独占全屏三种窗口模式。
- UE 返回的受支持分辨率列表，并保留当前分辨率选项。
- 低、中、高、史诗四档整体画质。
- “应用并返回”保存草稿；“放弃并返回”不修改已保存设置。

## 自动验证

- 编辑器与游戏 Development 构建成功。
- 设置专项测试见 `docs/verification/M4-settings-tests.json`：2/2 成功，0 警告、0 失败。
- Windows `BuildCookRun` 成功，候选包已更新。
- 启动烟雾见 `docs/verification/M4-settings-smoke.json`：进程持续运行 20.08 秒，无崩溃、断言、未处理异常或项目资源缺失。
- 原始日志含 5 条通用 `Failed to load`：4 个未安装的可选分析工具 DLL，以及未配置默认 `SoundConcurrencyObject` 的提示；均不影响候选启动。

## 人工验收清单

1. 按 Esc：世界暂停，暂停页可点击，战场不能接收命令。
2. 打开设置：将音量改为 35%，窗口模式和分辨率各切换一次，画质改为“中”。
3. 点击“应用并返回”，关闭程序并重新启动，确认四项值保留。
4. 再次修改四项后点击“放弃并返回”，确认已保存值未改变。
5. 在暂停页按 Esc 和点击“继续游戏”各测试一次，确认均能恢复战局。

用户已确认设置与暂停恢复路径无问题；M4-02 验收完成。最终交付范围已迁移至 M6-01。
