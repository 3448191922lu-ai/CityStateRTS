# M5-05 驻防与整补当前状态

## 状态

- 自动实现、候选交付与人工验收均已完成。
- 候选：`D:\ue project\RTS\Builds\Windows_M5_Garrison\RTS.exe`。

## 已实现

- 主城和普通城镇容量 2，激活要塞容量 3；要塞失效时不驱逐第三支既有驻军。
- 普通据点等待 3 秒后按小队初始总生命 3%/秒恢复，每 8 秒补一名满生命成员。
- 激活要塞立即按 5%/秒恢复，每 6 秒补一名满生命成员；整补免费且不改变人口占用。
- 右键或拖动小队徽记到己方据点可请求驻防；成员模型、碰撞和战斗关闭，但己方 HUD 徽记保留。
- 驻防徽记可选择和拖动；650 cm 内命令保持驻防，范围外移动、攻击移动或集火先出城再执行。
- 敌军进入 650 cm 时全部驻军从最近敌军一侧出城反击，并暂停当帧占领。
- AI 在无防守威胁时将生命低于 60% 的小队送往最近可用据点，等距优先要塞；达到 90% 后出城。
- AI 不读取驻防中的玩家小队作为可见野战目标。

## 自动证据

- RTSEditor Development 构建成功。
- `RTS.Strategy`：46/46 成功，0 warning、0 failure。
- BuildCookRun：`BUILD SUCCESSFUL`。
- 候选内部程序运行 20 秒，地图加载和 AI 决策均出现；致命错误、异常、断言、项目资源加载失败和网络错误均为 0。
- 包清单 49 个文件，SHA-256 复核 0 不匹配。
- 证据：`docs/verification/M5-garrison-tests.json`、`docs/verification/M5-garrison-smoke.json`、`docs/verification/M5-garrison-package.csv`。

## 人工验收

- 2026-09-09：用户确认当前候选没有问题；M5-05 验收通过。

1. 让受伤且缺员的小队右键己方普通城镇，确认到达后成员模型隐藏、徽记保留，等待 3 秒后恢复并每 8 秒补员。
2. 拖动驻防徽记到 650 cm 外地面和敌军，确认分别调出移动和攻击；拖到范围内确认继续驻防。
3. 普通城镇和主城各尝试第三支小队，确认容量为 2；激活要塞确认容量为 3。
4. 敌军进入据点占领范围，确认全部驻军从敌军方向出城反击且占领不会先推进一帧。
5. 观察低生命 AI 小队回城、整补至至少 90% 后重新行动。

## 已知维护项

- 最近五个 `Windows_*` 候选应保留到 `Windows_M4_VisualAudio`；`Windows_M3_NavFix4` 和 `Windows_M3_NavFix3` 已确认是超额旧候选。
- 当前命令安全策略拒绝递归删除，两个旧候选尚未移除；未尝试绕过策略。
