# M7 战术小地图当前状态

## 状态

- 战术小地图的代码、设置、自动化验证、Windows 候选和启动检查已完成。
- M7-01 当前为待人工验收；自动化结果不能替代鼠标交互与视觉检查。
- 当前候选：`D:\ue project\RTS\Builds\Windows_M7_Minimap\RTS.exe`。
- 两张遭遇战地图均已明确加入打包列表，默认启动仍进入“河谷争渡”。

## 已实现

- 右下角固定北向战术小地图，显示地面、河流、道路、据点、建筑、小队、驻防标记、迷雾和摄像机视野框。
- 己方对象始终显示；敌军仅在当前视野中显示；已探索城镇保留最后已知归属，不泄露视野外易手信息。
- 左键点击或拖动小地图移动摄像机，并继续使用原地图边界限制。
- 右键地面命令已选小队移动；右键当前可见敌军图标命令攻击；未选中小队时不下令。
- 暂停菜单“设置”新增“小地图方向”：`固定北向` 与 `跟随摄像机`。预览立即生效，应用后保存，放弃或按 Esc 返回时恢复原值。
- 拖动小地图期间打开暂停菜单或离开控件时会同步释放鼠标捕获，避免继续游戏后左右键失效。
- 720p 使用 `250×200 px`，1080p 及以上使用 `300×240 px`，不使用小数 RenderScale，避免重新引入 UI 抖动。

## 启动与操作

- 双击 `D:\ue project\RTS\Builds\Windows_M7_Minimap\RTS.exe` 默认进入“河谷争渡”。
- 原始地图可从 PowerShell 启动：
  `& 'D:\ue project\RTS\Builds\Windows_M7_Minimap\RTS.exe' '/Game/CityStateRTS/Maps/LVL_CityStateSkirmish'`
- 小地图左键点击或按住拖动：移动摄像机。
- 小地图右键地面：移动已选小队；右键可见敌军图标：攻击该目标。
- `Esc` → `设置` → `小地图方向`：切换方向模式。

## 自动证据

- RTSEditor Win64 Development 构建成功。
- 小地图专项规则测试：2/2 成功。
- `RTS.Strategy` 完整自动化：51/51 成功，0 failure，0 critical error。
- `Windows_M7_Minimap` BuildCookRun：`BUILD SUCCESSFUL`。
- 成品包显式包含“城邦争霸”和“河谷争渡”两张地图。
- 城邦地图成品程序运行 20 秒，停止前进程仍存活且响应正常；地图加载完成 1 次，Fatal、断言、未处理异常及 Slate/UMG 错误均为 0。
- 发布包清理运行期日志与本机配置后共 49 个文件，SHA-256 逐行复核 0 不匹配。
- 证据：`docs/verification/M7-minimap-tests.json`、`docs/verification/M7-minimap-smoke.json`、`docs/verification/M7-minimap-package.csv`。

## 人工验收

1. 在 `1280×720`、`1920×1080`、`1920×1200` 和当前超宽屏中确认小地图不遮挡底栏或城镇面板。
2. 检查固定北向默认模式及 `N` 标记；切换跟随摄像机后，底图、迷雾、图标、视野框和点击位置应同步旋转。
3. 检查己方对象、当前可见敌军、已探索城镇和驻防小队的显示规则，不应通过小地图获得视野外敌情。
4. 验证左键点击与拖动摄像机、右键移动、右键攻击，以及未选择小队时不下令。
5. 打开设置后分别执行应用、放弃和按 Esc 返回；继续游戏后左右键、滚轮和小地图交互均应正常。

以上通过后，将 M7-01 标记为完成，并继续 M6-01 的两张地图最终整局验收。
