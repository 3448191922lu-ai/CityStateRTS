# M5 当前状态

## M5-01 城镇专精与补给规则

- 状态：完成
- 已实现单级贸易、征募、要塞专精，250 金币、20 秒建设。
- 已实现 10 秒降级、100 金币退款、易手瘫痪、10 秒驻军重启和 5 秒进度回退。
- 已实现 7500 cm 同阵营据点补给图、派生经济、人口、入队时训练倍率和要塞攻击。
- 证据：`docs/verification/M5-territory-rules.json`。

## M5-02 城镇管理与世界反馈

- 状态：完成
- 已实现原生 UMG 城镇面板、城镇选择、专精和降级按钮。
- 已实现专精图标、工程进度、断链标记和仅玩家可见的补给线。
- 敌方建设中的专精选择不会提前显示；敌方补给和迷雾外实时状态不公开。
- 修复了独立游戏模式中据点先于比赛状态加载表现数据导致的启动崩溃。
- 修复了城镇面板画布插槽未采用 760x176 期望尺寸，导致文字溢出和竖排的问题。
- 编辑器构建成功，领土定向测试 9/9 通过；更新后的打包候选运行 10.5 秒无致命错误、项目资源加载错误或网络错误。
- 原生窗口未向自动化接口暴露，因此鼠标选择、按钮点击、右键命令和暂停菜单共存仍需人工交互验收。
- 证据：`docs/verification/M5-territory-ui.json`、`docs/verification/M5-town-panel-ui-fix.json`。

## M5-03 公平 AI 城镇发展

- 状态：完成
- 已实现贸易、征募、要塞的固定专精优先级，以及金币至少 500 时对被继承错配专精的降级重建。
- AI 优先防守当前可见威胁，再驻军重启占领后瘫痪的城镇；所有专精与降级均调用玩家共用的比赛状态接口。
- 威胁和克制训练只读取 AI 当前可见的玩家小队，不读取迷雾外单位；据点使用固定公开位置。
- 编辑器构建成功，领土定向测试 9/9 通过，报告内 0 warning、0 failure。
- 有效无人干预观察约 77 秒：AI 执行占领，据点从 1 增至 2，收入达到 8/秒，并选择首个贸易专精；窗口由用户主动关闭。用户取消了重复五分钟观察，因此第二专精及普通建造、训练、进攻未在本轮观察中确认。
- 证据：`docs/verification/M5-territory-tests.json`、`docs/verification/M5-ai-observation.json`。

## M5 Windows 候选

- 候选路径：`D:\ue project\RTS\Builds\Windows_M5_Territory\RTS.exe`。
- BuildCookRun：`BUILD SUCCESSFUL`；启动器与内部游戏程序均已生成。
- 包清单：49 个发布文件，逐文件记录相对路径、字节数和 SHA-256；运行时生成的日志与 `RTS/Saved` 文件不计入清单，见 `docs/verification/M5-package.csv`。
- UI 修复后打包烟雾：内部游戏程序运行 10.5 秒，停止前仍存活；致命错误、异常、断言、项目资源缺失、网络错误和普通错误行均为 0。
- 版本保留：仅保留最新 5 个 `Windows_*` 候选；10 个更旧候选已永久删除且不可恢复。
- 证据：`docs/verification/M5-smoke.json`、`docs/verification/M5-package.csv`、`docs/verification/M5-build-retention.json`。

## M5 打包版人工验收清单

1. 在两张地图分别占领一座城镇，确认可以选择贸易、征募或要塞专精。
2. 建设期间制造争夺，确认进度暂停；完成后确认专精主体效果。
3. 占领中继城镇建立连接，再失去它切断远端城镇，确认只失去补给加成。
4. 攻占一个已有专精的敌方城镇，确认专精保留但瘫痪，驻军 10 秒后重新启用。
5. 降级一座城镇，确认完成后返还 100 金币，并可重新支付 250 金币选择新专精。
6. 比较征募城领地内外的新训练队列；确认要塞射击、15 秒占领和迷雾信息限制。
7. 观察 AI 完成占领、至少两种专精、生产、进攻，以及一次城镇防守或重新启用。
8. 两张地图各完成一整局，确认胜负和 R 重开仍然正常。

## M5-05 驻防与整补

- 状态：完成
- 主城和普通城镇最多驻防 2 支小队，激活要塞城镇最多驻防 3 支。
- 普通据点等待 3 秒后按初始总生命 3%/秒恢复并每 8 秒补员；激活要塞立即按 5%/秒恢复并每 6 秒补员。
- 驻防仅隐藏成员模型并保留己方小队 HUD；范围外命令调出，敌军进入占领范围时全体出城反击。
- AI 使用相同容量与整补接口，在低于 60% 时回城并于达到 90% 后出城。
- 玩家命令、驻防 HUD、容量、整补、反击和公平 AI 已接通；完整 `RTS.Strategy` 46/46 成功。
- `Windows_M5_Garrison` BuildCookRun 成功，20 秒启动审计无致命错误、项目资源加载失败或网络错误；49 个包文件哈希复核 0 不匹配。
- 用户于 2026-09-09 确认候选没有问题，M5-05 人工验收完成。
- 详细状态与人工清单：`docs/M5-GARRISON-CURRENT.md`。
